extends layout

block content
  - let items = results
  h1= title
  .container-fluid 
    .row 
      each cin in items
        div(class="col-md-2" style=`border:dotted 3px grey` onclick=`buy(${items.indexOf(cin)})`) 
          p.
           peacock_species -  #{cin.peacock_species}
          p.
           feather_color -  #{cin.feather_color}
          p.
           age  -  #{cin.age}
  script.
    function buy(index){
      alert("You purchased item "+index)
    }